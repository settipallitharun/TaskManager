{
  "name": "ai-team-task-manager",
  "version": "1.0.0",
  "description": "Production-grade full-stack SaaS web application for team task management",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently -n \"API,WEB\" -c \"blue,green\" \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "node server/index.js",
    "setup": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": [
    "saas",
    "task-management",
    "react",
    "nodejs",
    "postgresql"
  ],
  "author": "AI Team Task Manager",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}